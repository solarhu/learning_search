# AI Assistant Rules

## Testing Requirements

1. **每个接口需要有测试用例** - 所有 API 接口、服务类、工具函数都需要编写对应的测试用例

2. **每次修改需要新增测试用例** - 当修改现有代码时，必须：
   - 为新增的功能添加新的测试用例
   - 更新受影响的现有测试用例
   - 运行测试确保所有测试通过

3. **测试执行** - 在提交代码前，必须运行测试命令：
   - Go 后端: `go test ./...` (在 server 目录下)
   - Flutter 前端: `flutter test` (在项目根目录)

4. **测试覆盖率** - 关键业务逻辑需要有完整的测试覆盖

## Code Style

- 不添加注释（除非用户明确要求）
- 保持代码简洁
- 遵循项目现有的代码风格和命名规范

## Git Commit

- 只在用户明确要求时才创建 commit
- commit 消息要简洁，描述改动的原因而非内容